name: clue_release
channels:
  - defaults
dependencies:
  - python==3.12.*
  - pip
  - requests
  - flake8
  - pytest
  - pylint

  - pip:
      - django>=5.0.1
      - daphne>=4.1.2
      - websocket-client>=1.8.0
      - channels>=4.1.0